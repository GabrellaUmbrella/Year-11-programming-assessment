def add_one(num):
    return num + 1

my_list = [3,78,2,56,7]

for num in my_list:
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    num = add_one(num)
    print(add_one(num))